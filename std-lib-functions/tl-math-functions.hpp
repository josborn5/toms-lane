namespace tl {
	float square_root(float input);
	float sine(float input_radians);
	float cosine(float input_radians);
	float tangent(float input_radians);
	float arctangent(float input_radians);

	void put_string(const char* log);
	void print_int(const char* log, int num);
}
