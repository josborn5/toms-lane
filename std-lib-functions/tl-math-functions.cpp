#include <math.h>

namespace tl {
	float square_root(float input) {
		return sqrtf(input);
	}
	float sine(float input) {
		return sinf(input);
	}
	float cosine(float input) {
		return cosf(input);
	}
}
